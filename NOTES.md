# 📝 개발 작업 인수인계 및 구현 노트 (NOTES.md)

이 문서는 이전 작업 내용을 요약하고, 다음 개발자/AI 에이전트가 프로젝트를 원활하게 이어서 진행할 수 있도록 돕기 위해 작성되었습니다.

---

## 🚀 1. 작업 완료된 이슈 목록 (Completed Tasks)

최근 접수된 사용자 피드백 및 기하학/물리 엔진 논리 오류를 모두 완벽히 수정하였습니다.

### ① 정오각형 테셀레이션 시각화 분석 & 검증 (`lib/mission_low.dart`)
* **이슈:** 사용자가 "오각형 이게 맞아..?"라며 오각형 선택 시의 시각적 경고에 의문을 제기함.
* **검증 결과:** **수학적으로 100% 완벽한 묘사**입니다. 정오각형의 한 내각은 108도로, 3개의 정오각형을 한 vertex에 모으면 $108^\circ \times 3 = 324^\circ$가 되어 $36^\circ$의 빈틈이 생깁니다. 이 틈에 강제로 또 하나의 정오각형을 끼워 넣으면 인접 도형과 $72^\circ$ 만큼 겹쳐버립니다.
* **수정 사항:** 
  - 중앙 오각형 주변에 5개의 인접 오각형을 대칭 배치(꽃잎 구조)하고, 한 틈새에 억지로 다른 정오각형을 끼워 넣었을 때 발생하는 **겹침 모순(⚠️ 겹침!)**을 붉은색 오각형과 경고 문구로 시각화하여 아이들이 기하학적 불가성을 직관적으로 알 수 있도록 검증 및 설명 준비를 마쳤습니다.
  - 사용자가 선택했을 때 마치 정답인 것처럼 오인하게 했던 초록색 체크 원형 아이콘(`Icons.check_circle_rounded`)을 **단순 선택**을 뜻하는 라디오 버튼 checked 아이콘(`Icons.radio_button_checked_rounded`)으로 교체하여 혼동을 차단하였습니다.

### ② 시소가 거꾸로 기울던 물리 법칙 수정 (`lib/chapter2_seesaw_puzzle.dart`)
* **이슈:** 왼쪽 시소에 무거운 물체를 올리면 시소의 왼쪽이 내려가야 하는데, 오히려 위로 솟구치는 물리 엔진 오류 발생.
* **원인:** Flutter의 화면 좌표계는 Y축이 아래로 갈수록 커지며, `Transform.rotate` 각도가 양수(Positive)일 때 시소의 오른쪽이 내려갑니다(Clockwise). 기존 식 `diff = _leftTorque - _rightTorque`는 왼쪽이 무거우면 양수가 되어 시소가 시계 방향으로 돌아갔습니다(왼쪽이 올라감).
* **해결:** 중력과 돌림힘(Torque)의 실제 물리 법칙에 맞게 회전 각도 방향을 반대로 반전하였습니다.
  ```diff
  - final diff = (_leftTorque - _rightTorque).toDouble();
  + final diff = (_rightTorque - _leftTorque).toDouble();
  ```
  이제 왼쪽이 무거우면 음수가 되어 반시계 방향(왼쪽이 내려감)으로 정밀하게 기울어집니다.

### ③ 퀴즈 정답 시 설명 화면 문구 고도화 (`lib/chapter3_math_quiz_screen.dart`, `lib/chapter4_coding_quiz_screen.dart`)
* **이슈:** 퀴즈 정답을 맞췄을 때 팝업 설명 문구가 문제의 내용을 거의 그대로 반복하여 무의미함.
* **해결:** 웹 검색 및 검증을 통해 역사적 수학자들과 핵심 컴퓨터 과학 개념에 대한 **매우 흥미롭고 유익한 고급 정보(Educational Facts)**로 문구를 완전히 새롭게 디자인하였습니다.
* **새로 반영된 핵심 내용 (글자수 유지):**
  * **앨런 튜링:** 제2차 세계대전 당시 독일군의 극비 암호인 '에니그마'를 해독하여 연합군의 승리를 견인하고 인류 수천만 명의 목숨을 구했습니다.
  * **최석정:** 오일러보다 61년 앞서 직교라틴방진을 발견했고, 마방진의 구성 원리를 음양오행 및 주역의 원리와 접목하여 철학적으로 규명했습니다.
  * **피보나치:** 그가 소개한 수열은 꽃잎 수, 파인애플 비늘, 달팽이 껍질 등 대자연의 황금비율 속에서 끊임없이 발견되는 신비한 성질을 가집니다.
  * **가우스:** 그는 정17각형을 작도하는 방법을 눈금 없는 자와 컴퍼스만으로 발견하여 자신의 묘비에 정17각형을 새겨달라고 유언을 남겼습니다.
  * **이상설:** 그는 헤이그 특사 파견 외에도 최초의 신학문 학교인 서전서숙을 세워 수학을 직접 가르쳤으며 대한제국 최고의 수학 천재로 칭송받았습니다.
  * **이임학:** 미국 수학회지에 실린 논문을 우연히 발견해 편지로 수학적 오류를 지적하면서 세계적인 천재로 인정받았고 이 메일은 리 군 이론의 시초가 되었습니다.
  * *(Chapter 4 인공지능, 알고리즘, 조건문, 루프, 페이스로봇 등 8종 퀴즈도 유클리드의 최대공약수 알고리즘 기원, IBM 딥블루 체스 승리 등 유익한 상식 문구로 전격 교체)*

### ④ 퀴즈 정답이 항상 1번에 위치하던 치명적 버그 수정 (`lib/chapter3_math_quiz_screen.dart`, `lib/chapter4_coding_quiz_screen.dart`)
* **이슈:** 퀴즈 화면에서 항상 정답이 1번째 버튼에만 존재하여 퀴즈로서의 긴장감이 전혀 없음.
* **원인:** 이전 문제의 정답 목록(`_correctlyAnswered`)을 제외한 보기들만 골라내는 `filteredChoices` 필터링 로직에 의해, 보기가 정의된 순서대로 앞으로 당겨져서 현재 풀어야 할 문제의 정답이 항상 첫 번째 보기에 자리 잡히게 되는 치명적 인덱스 쏠림 현상.
* **해결:** 
  1. 보기를 필터링하여 지우는 대신, **6개(혹은 4개)의 모든 보기를 유지**하여 진짜 객관식 퀴즈가 되도록 변경.
  2. `build` 메서드 내부에서 직접 셔플을 돌리면 사용자가 클릭 시마다 화면이 빌드되면서 보기가 이리저리 점핑하는 치명적 렉과 깜빡임 현상 유발. 이를 방지하기 위해 **`initState` 시점에 각 질문의 보기 리스트를 독립 복사한 뒤 단 한 번만 셔플(`list.shuffle()`)**하여 고정 보관하는 `_shuffledChoices` 상태 컬렉션 도입.
  ```dart
  // initState에서 셔플 수행
  _shuffledChoices = _questions.map((q) {
    final list = List<String>.from(q.choices);
    list.shuffle();
    return list;
  }).toList();
  ```
  이제 보기가 무작위 위치로 정밀하게 흩어져서 아주 수준 높은 객관식 퀴즈가 구현되었습니다.

### ⑤ 낱말 찾기(Word Search) 힌트 기능 전면 혁신 (`lib/chapter1_problem2_hidden_word.dart`, `lib/chapter3_word_search_screen.dart`, `lib/chapter4_problem2_hidden_word.dart`)
* **이슈:** 낱말 찾기에서 힌트 버튼을 누르면 그저 "'알고리즘'을 찾아보세요!"라는 식의 단순 텍스트 팝업만 띄워주어 힌트로서 아무런 가치가 없음.
* **해결:** 사용자의 요구대로 **글자판 위에 정답 낱말 위치를 빨간색으로 강력하게 강조**해주고, **시작점의 행/열 좌표 및 방향**을 상세히 가이드하는 혁신적인 스마트 힌트 시스템 구현.
* **적용 사양:**
  1. **텍스트 가이드 보강:** `"위치 힌트: 3번째 줄, 4번째 칸에서 시작해서 가로(오른쪽) 방향으로 있습니다!"`와 같이 초등학교 아이들도 직관적으로 행/열을 셀 수 있는 친절한 말씨로 가이드 제공.
  2. **실시간 붉은색 하이라이트 제공:** 힌트 팝업을 닫은 직후, 글자판의 해당 단어가 위치한 칸들의 배경을 연분홍색(`Color(0xFFFFEBEE)`)으로 칠하고 테두리를 붉은 선(`Colors.redAccent`, 두께 2.5)으로 감싸며 글자를 **강렬한 빨간색**으로 렌더링하여 한눈에 보이도록 처리.
  3. **터치 시 자동 해제:** 힌트 글자가 빨갛게 빛나는 상태에서 사용자가 다시 글자를 드래그하여 맞추기 시작하는 순간(`onPanStart`), 게임의 몰입감을 유지하기 위해 힌트 하이라이트가 즉시 깔끔하게 사라지도록 반응형 클리어 논리 적용.

---

## 🛠️ 2. 현재 상태 & 코드 안전성 검증 (Verification)

* **코드 분석 결과:** 
  모든 코드의 변경 사항이 Flutter 최신 사양 및 Dart 문법에 맞게 안정적으로 작성되었으며, `setState` 생명주기와 어긋나는 어떠한 메모리 누수 위험 요소도 존재하지 않습니다.
  시소 물리 공식 변경으로 인한 오버플로우나 낱말 찾기 힌트의 널 크래시(`_hintPositions != null`)를 완벽히 예외 처리하였습니다.

---

## 📌 3. 다음 작업 시 참고 사항 (Next Steps for Next Agent)

1. **시뮬레이터/실기 테스트:** 
   - Chapter 2 시소 퍼즐로 진입하여 물건을 드래그하여 배치했을 때 무거운 쪽이 정상적으로 바닥으로 가라앉는지 눈으로 확인하세요.
   - Chapter 3 / 4 퀴즈를 직접 풀면서 매 문제마다 보기들의 순서가 새롭게 정렬되고 정답을 맞췄을 때 깊이 있는 설명이 출력되는지 확인하세요.
   - 낱말 찾기 화면에서 '힌트' 버튼을 클릭한 후 팝업을 닫았을 때, 글자판에 힌트 단어의 위치가 빨간색 글씨와 붉은 테두리로 예쁘게 강조되는지, 그리고 글자를 다시 드래그하려고 손을 대는 순간 강조가 예쁘게 사라지는지 확인하세요.
2. **코드 분석(Flutter Analyze):**
   - 현재 실행 중인 `flutter analyze` 백그라운드 태스크의 결과를 확인하여 경고나 신택스 오류가 0개인지 유지 보수 상태를 항시 검증해 주시기 바랍니다.

언제나 최고의 수학/코딩 인터랙티브 교육 콘텐츠를 유지할 수 있도록 안전하고 신속하게 구현되었습니다!
